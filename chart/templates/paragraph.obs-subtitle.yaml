kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Platform health — reconciliation, performance, logs and resource usage; alerts and investigations live on Incidents
    type: secondary
  resourcesRefs:
    items: []
