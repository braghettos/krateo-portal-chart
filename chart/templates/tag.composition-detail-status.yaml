# Status pill next to the title. The conditions→label/color logic lives HERE
# (widget representation), not in the RA: the RA just supplies the raw object.
# Honest rollup: a Ready=True comp whose Synced=False is DRIFTED, not Healthy —
# surface "Drift" (magenta) so a reconcile-paused/diverged comp isn't mislabelled
# green. Mirrors the compositions-list state language (Healthy/Drift/Degraded/Pending).
kind: Tag
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: composition-detail-status
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  # F6 (snowplow#130): extras keys that partition this widget's cache cell.
  keyExtras: [name, namespace]
  apiRef:
    name: composition-detail
    namespace: {{ .Release.Namespace }}
  widgetData:
    label: Unknown
    color: default
  # Title tag = the composition's REAL `Ready` condition (not a derived Healthy/Drift label):
  # Ready=True → "Ready" (green — health), Ready=False → "Not ready" (red), absent → "Unknown" (gray).
  # ONE override outranks Ready: the engine's workload projection. workloadHealthy == false
  # (crashlooping pods behind a Ready=True comp) → "Unhealthy" (red) — the pill must not
  # read green while the actual workload is down. (.workloadHealthy = RA-exposed raw field.)
  widgetDataTemplate:
    - forPath: label
      expression: '${ (.detail.status.conditions // []) as $c | ($c | map(select(.type == "Ready")) | .[0]) as $r | if .workloadHealthy == false then "Unhealthy" elif $r == null then "Unknown" elif $r.status == "True" then "Ready" else "Not ready" end }'
    - forPath: color
      expression: '${ (.detail.status.conditions // []) as $c | ($c | map(select(.type == "Ready")) | .[0]) as $r | if .workloadHealthy == false then "red" elif $r == null then "gray" elif $r.status == "True" then "green" else "red" end }'
  resourcesRefs:
    items: []
