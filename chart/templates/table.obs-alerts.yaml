# HyperDX alerts status — lists the Alert CRs (observability.krateo.io) with their live state,
# from restaction.obs-alerts. state is colour-coded: ALERT crimson, PENDING amber, OK cyan. The
# numbers/threshold are the real alert config; state is mirrored from HyperDX by the reconciler.
# Lives on /incidents since the IA split (page-observability keeps only platform health).
kind: Table
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-alerts
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    allowedResources: []
    bordered: false
    size: small
    # Row click → the Autopilot rail seeded to investigate THAT alert (same destination the
    # troubleshooting feed beside it uses). The seed sentence rides in a hidden `ask`
    # cell (built server-side by restaction.obs-alerts); the Table URI-encodes interpolated
    # cells, so {ask} lands as a proper ?ask= query value. Retargeted to /incidents (IA split).
    rowNavigateTo: /incidents?ask={ask}
    pagination:
      defaultPageSize: 10
    columns:
    - valueKey: name
      title: Alert
    - valueKey: watching
      title: Watching
    - valueKey: threshold
      title: Threshold
    - valueKey: interval
      title: Every
    - valueKey: state
      title: State
    dataSource: []
  widgetDataTemplate:
  - forPath: dataSource
    expression: '${ [ (.rows // [])[] | [ {valueKey:"name",kind:"jsonSchemaType",type:"string",stringValue:.name}, {valueKey:"ask",kind:"jsonSchemaType",type:"string",stringValue:(.ask // "")}, {valueKey:"watching",kind:"jsonSchemaType",type:"string",stringValue:(.watching // "-"),color:"#8c8c8c"}, {valueKey:"threshold",kind:"jsonSchemaType",type:"string",stringValue:((.thresholdType // "") + " " + (.threshold // ""))}, {valueKey:"interval",kind:"jsonSchemaType",type:"string",stringValue:(.interval // "-"),color:"#8c8c8c"}, {valueKey:"state",kind:"jsonSchemaType",type:"string",stringValue:(.state // "PENDING"),color:((.state // "PENDING") | if . == "ALERT" then "#cf1322" elif . == "PENDING" then "#d48806" else "#13a8a8" end)} ] ] }'
  apiRef:
    name: obs-alerts
    namespace: {{ .Release.Namespace }}
  resourcesRefs:
    items: []
