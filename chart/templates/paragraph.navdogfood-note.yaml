kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: navdogfood-note
  namespace: krateo-system
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: "This page and its sidebar entry were published together via the KOG git-write path; the nav entry shipped as a files/nav-fragments/ fragment (#106), with no manual menu edit."
  resourcesRefs:
    items: []
