# Full-width strip hosting the obs-troubleshooting-card.
kind: Row
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-troubleshooting-row
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    allowedResources:
    - cards
    items:
    - resourceRefId: obs-troubleshooting-card
      size: 24
  resourcesRefs:
    items:
    - id: obs-troubleshooting-card
      apiVersion: widgets.templates.krateo.io/v1beta1
      name: obs-troubleshooting-card
      namespace: {{ .Release.Namespace }}
      resource: cards
      verb: GET
