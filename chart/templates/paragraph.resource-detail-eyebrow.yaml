# /resources page eyebrow — the object's apiVersion (data-driven from resource-detail).
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: resource-detail-eyebrow
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  keyExtras: [namespace, group, version, plural, name]
  apiRef:
    name: resource-detail
    namespace: {{ .Release.Namespace }}
  widgetData:
    text: ""
    variant: eyebrow
  widgetDataTemplate:
    - forPath: text
      expression: '${ .eyebrow // "" }'
  resourcesRefs:
    items: []
