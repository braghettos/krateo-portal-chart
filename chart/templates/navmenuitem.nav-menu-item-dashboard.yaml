kind: NavMenuItem
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: nav-menu-item-dashboard
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    allowedResources:
      - flexes
    resourceRefId: dashboard-flex
    label: Dashboard
    icon: fa-chart-line
    path: /dashboard
    order: 10
  resourcesRefs:
    items:
      - id: dashboard-flex
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: dashboard-flex
        namespace: {{ .Release.Namespace }}
        resource: flexes
        verb: GET