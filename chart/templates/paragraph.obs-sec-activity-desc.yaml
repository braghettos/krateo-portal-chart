# Observability section header — obs-sec-activity-desc
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-activity-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Recent composition events across the platform.'
    type: secondary
  resourcesRefs:
    items: []
