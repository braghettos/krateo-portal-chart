kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: settings-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    # Exact mockup copy (settings .pg-head subtitle).
    text: Manage your Krateo platform and access.
    type: secondary
  resourcesRefs:
    items: []
