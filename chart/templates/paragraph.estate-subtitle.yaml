kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: estate-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Every composition and the resources it manages — health at a glance
    type: secondary
  resourcesRefs:
    items: []
