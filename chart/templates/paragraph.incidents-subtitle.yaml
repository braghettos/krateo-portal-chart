kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: incidents-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: One row per investigation — root cause, lifecycle and gated remediation for every analysis
    type: secondary
  resourcesRefs:
    items: []
