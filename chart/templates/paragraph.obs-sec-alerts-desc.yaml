# Observability section header — obs-sec-alerts-desc
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-alerts-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'HyperDX alerts as Kubernetes resources — with an automatic Autopilot root-cause analysis the moment one fires.'
    type: secondary
  resourcesRefs:
    items: []
