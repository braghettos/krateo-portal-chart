kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: clusters-title
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Clusters
    strong: true
  resourcesRefs:
    items: []
