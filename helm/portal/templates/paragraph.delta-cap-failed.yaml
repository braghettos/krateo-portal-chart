kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: delta-cap-failed
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: needs attention
    type: danger
  resourcesRefs:
    items: []
