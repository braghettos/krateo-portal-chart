kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: alerts-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Alert rules watching your telemetry — threshold, interval and current state. A firing alert opens on the Incidents page; select a row to investigate it in Autopilot.
    type: secondary
  resourcesRefs:
    items: []
