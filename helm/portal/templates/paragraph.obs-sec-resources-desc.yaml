# Observability section header — obs-sec-resources-desc
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-resources-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Live pod CPU & memory from OpenTelemetry metrics.'
    type: secondary
  resourcesRefs:
    items: []
