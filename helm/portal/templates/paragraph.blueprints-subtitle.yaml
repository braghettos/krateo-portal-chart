kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: blueprints-subtitle
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: Reusable templates — install a blueprint to create a composition (a running instance) in your namespace.
    type: secondary
  resourcesRefs:
    items: []
