# Observability section header — obs-sec-performance-desc
kind: Paragraph
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: obs-sec-performance-desc
  namespace: {{ .Release.Namespace }}
  annotations:
    "krateo.io/verbose": "true"
spec:
  widgetData:
    text: 'Reconcile timing from OpenTelemetry traces & metrics — throughput, latency, percentiles and where the time goes.'
    type: secondary
  resourcesRefs:
    items: []
